{
  "name": "cayman-motorcycle-handbook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:offline": "vite build --config vite.offline.js && mkdir -p public/downloads && cp dist-offline/index.html public/offline.html && cp dist-offline/index.html public/downloads/CIMRA-Handbook-Offline.html && cd public/downloads && zip -q -r CIMRA-Handbook-Offline.zip CIMRA-Handbook-Offline.html",
    "native:sync": "npm run build && npx cap sync",
    "native:android": "npm run native:sync && npx cap open android",
    "native:ios": "npm run native:sync && npx cap open ios",
    "native:android:release": "npm run native:sync && cd android && ./gradlew bundleRelease",
    "native:ios:archive": "npm run native:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0",
    "lucide-react": "^0.468.0",
    "qrcode.react": "^4.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.3",
    "vite-plugin-singlefile": "^2.3.2"
  }
}
